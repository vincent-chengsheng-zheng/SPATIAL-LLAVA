# ── NOTE ──────────────────────────────────────────────────
# torch and torchvision are installed separately:
#   - Dockerfile.dev : CPU-only wheels (Mac local dev)
#   - Cluster        : pre-installed with CUDA support
# Do NOT add torch here.
# ──────────────────────────────────────────────────────────

# ── ML dependencies ────────────────────────────────────────
peft==0.7.1
transformers==5.4.0
bitsandbytes==0.41.1
accelerate==1.13.0
safetensors==0.7.0

# ── Data processing ────────────────────────────────────────
datasets==4.8.4
Pillow-SIMD
opencv-python-headless==4.8.1.78
tqdm==4.67.3
pyyaml==6.0.1
numpy==1.26.4

# ── Web / deployment ───────────────────────────────────────
fastapi==0.104.1
uvicorn==0.24.0
python-multipart==0.0.6

# ── Dev / testing ──────────────────────────────────────────
pytest==7.4.3
pytest-cov==4.1.0
flake8==6.1.0

sentencepiece==0.2.0
tiktoken==0.9.0
protobuf==5.29.4
blobfile==3.0.0
scipy==1.17.1

# flash-attn requires separate install:
# pip install flash-attn --no-cache-dir --no-build-isolation
gradio
